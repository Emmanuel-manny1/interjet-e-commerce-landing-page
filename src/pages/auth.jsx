import { Photo } from "../Components/photo"
import { Form } from "../Components/form"

export const Auth = () => {
    return(
        <div className="app-container">
        <Photo />
        <Form />
        </div>
    )
}